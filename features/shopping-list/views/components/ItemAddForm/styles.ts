import { StyleSheet } from 'react-native';

export const itemAddFormStyles = StyleSheet.create({
    inputContainer: {
        flexDirection: 'row',
    },
    input: {
        flex: 1,
        borderWidth: 1,
        borderColor: '#ddd',
        padding: 10,
        marginRight: 10,
        borderRadius: 5,
        backgroundColor: '#fff',
    },
});
