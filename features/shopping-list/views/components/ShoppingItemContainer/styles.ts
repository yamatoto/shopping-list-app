import { StyleSheet } from 'react-native';

export const shoppingItemContainerStyles = StyleSheet.create({
    quantityContainer: {
        backgroundColor: '#f0f0f0',
        borderRadius: 5,
        padding: 5,
        minWidth: 30,
        alignItems: 'center',
    },
    quantityText: {
        fontSize: 16,
        fontWeight: 'bold',
    },
});
